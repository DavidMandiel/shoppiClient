import { CalculateCostPipe } from './calculate-cost.pipe';

describe('CalculateCostPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateCostPipe();
    expect(pipe).toBeTruthy();
  });
});
