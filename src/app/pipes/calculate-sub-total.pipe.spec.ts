import { CalculateSubTotalPipe } from './calculate-sub-total.pipe';

describe('CalculateSubTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateSubTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
